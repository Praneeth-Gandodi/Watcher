# Unit tests

Implementation agents add focused tests inside ownership-named subdirectories:

- `negotiation/`
- `allocation/`
- `safety/`
- `dashboard/`

Tests must use deterministic simulation time and fixtures.
